# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PullToRefresh' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PullToRefresh
  
end

target 'PullToRefreshDemo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PullToRefreshDemo
  pod 'SnapKit'

  # debug pods
  # pod 'FLEX', '~> 2.0', :configurations => ['Debug']
  # pod 'GDPerformanceView-Swift', :configurations => ['Debug']
  # pod 'Reveal-SDK', '~> 13.0', :configurations => ['Debug']
  # UI调试工具
  pod 'LookinServer', :configurations => ['Debug']
  # 网络调试工具
  #pod 'Bagel', '~>  1.3.2', :configurations => ['Debug']
  # 多功能的调试工具
  pod 'WoodPeckeriOS', :configurations => ['Debug']
  # 一款功能齐全的客户端研发助手
  pod 'DoraemonKit/Core', '~> 1.2.2', :configurations => ['Debug']
  pod 'DoraemonKit/WithLogger', '~> 1.2.2', :configurations => ['Debug']
  pod 'DoraemonKit/WithLoad', '~> 1.2.2', :configurations => ['Debug']
  #pod 'DoraemonKit/WithGPS', '~> 1.2.2', :configurations => ['Debug']
  #pod 'DoraemonKit/WithWeex', '~> 1.2.2', :configurations => ['Debug']
  #pod 'DoraemonKit/WithDatabase', '~> 1.2.2', :configurations => ['Debug']
end
